# DP-01 — Founder Authorization Surface for P11

> # SUPERSEDED BY ISSUANCE — 2026-09-11
>
> **The Founder has since issued `DP-01`.** `P11 AUTHORIZED = TRUE`.
> The issued instrument is
> [`docs/governance/acts/DP-01-P11-FOUNDER-AUTHORIZATION.md`](../../governance/acts/DP-01-P11-FOUNDER-AUTHORIZATION.md)
> and **it, not this document, is the authority.**
>
> This file is a **prepared surface** and is retained **as history**. Everything
> below it was true when written and is left unaltered. Its
> `P11 AUTHORIZED = FALSE` is an **as-of-2026-09-10 statement, not a current
> claim** — do not cite it as standing state.
>
> `DP-01 §0` confirms this document's central finding: the issuance is
> *"not a reconstruction, recovery, inferred decision, or retrospective
> interpretation of a previously existing DP-01 instrument."*
> **`PREPARED SURFACE ≠ AUTHORIZATION`.**
>
> **Identifier warning (`SD-12`):** the name `DP-01` denotes three different
> things across this programme. This file is the *prepared surface*; the issued
> Founder authorization is the file linked above. The collision is escalated to
> the Founder and remains unresolved.

---

> **Prepared under `ACT-CC-P11-004`** — 2026-09-10.
> **`P11 AUTHORIZED = FALSE`.** This is a **prepared surface**, not an
> authorization. `§18`: **`PREPARE ≠ DECIDE`.**
>
> **Founder-reserved fields are marked and unfilled.**

---

## 0. The finding that governs this Act

> ## **There is no existing `DP-01` instrument. There never was one.**

`§4` required me to locate and read *"the actual current body of DP-01."*
**It does not exist.** Searched by filename across the tree and by content across
the corpus. Every occurrence of `DP-01` in the P11 track is a **forward reference
to a decision not yet taken** — in my own records and inside `DP-03`'s body — not
a pointer to an instrument.

**This is not a lost source.** `§4`'s contingency assumes a body that cannot be
found; here the body was never authored, which is the ordinary condition of a
decision nobody has made yet. **The distinction matters for what I may do next:**

- **`§4` forbids reconstructing an authorization.** Nothing is reconstructed —
  there is no prior text to reconstruct, and none is invented.
- **`§24` and `§27 F` require preparing the surface.** That is done, built from
  **issued** `DP-03` and `DP-04` plus the resident Blueprint.

**Consequence for `§16`:** the scope diff has **nothing on its left-hand side.**
Every row is **`ADD`**. There is no clause to `PRESERVE`, `MODIFY` or `REMOVE`,
and I will not manufacture a "previous version" to compare against.

## 0.1 A `DP-01` identifier collision, reported under `§23`

**`DP-01` carries at least three distinct meanings in the resident corpus:**

| Meaning | Location |
|---|---|
| *"Authority Before **Decision**"* — a **Decision Principle** | `volume-1/pd-01-executive-office/C2.md:73`, `§4 Decision Principles` |
| *"Authority Before **Delegation**"* — a **Delegation Principle** | `volume-1/pd-01-executive-office/C3.md:70`; also `volume-2/pd-02-architecture-office/C3.md:114` |
| **Founder P11 Authorization** | the P11 track — this document |

**Two of them differ inside the same volume**, section to section. `§23` warns
that no document wins merely because a filename or summary says so, and
`IDENTIFIER ≠ ACTUAL DECISION BODY` — **this is that invariant with teeth:** a
search for `DP-01` returns PD principles that have nothing to do with authorizing
P11.

**Reported, not renamed.** Renaming a Founder identifier is not a delegated act.
**And one of the collisions is substantively useful:** PD-01's
`DP-01 — Authority Before Delegation` — *"Hanya pihak dengan authority resmi yang
dapat mendelegasikan"* — is the same principle `DP-03 §11` encodes as
`DELEGATION ≠ AUTHORITY CREATION`.

---

## 1. Founder identity

**Founder / Program Owner:** Moriarty.

## 2. The authorization question

> **Does the Founder authorize construction of Phase 11 — Autonomous
> Organization — over the architectural surface fixed by `DP-04` and `DP-03`, and
> no wider?**

## 3. P11 objective

Build a **bounded** autonomous organization above the certified P10 Department
Ecosystem, able to plan, delegate, execute, coordinate, observe, verify, escalate
and hold accountability — **without expanding constitutional or Founder
authority** (`P11-W7`).

---

## 4. Authorized scope `W1`–`W7`, bounded by the issued architecture

| WP | Authorized surface | Source | May construct | May **not** |
|---|---|---|---|---|
| **W1** Coordination | **Workflow** | `DP-03 §8.1` CONFIRMED | cross-department coordination · work routing · dependency · handoff · escalation | create coordination authority contrary to governance/ownership |
| **W2** Planning | **Organizational-layer Planning surface** | `DP-03 §8.4` | goal decomposition · planning · sequencing · dependency-aware planning · **plan lifecycle** · adaptation · revision | self-authorize · create governance/constitutional/Founder authority · override architecture · bypass Workflow |
| | **Mandatory lifecycle** | `DP-03 §8.4` | `PLAN → SEQUENCE → ADAPT → REVISE` | **be constrained to a static P10-style declaration** |
| **W3** Delegation | **Organizational-layer governed relation/record** | `DP-03 §8.2`, `DP-04 §8.3` | delegation representation · authority boundary · accountability · tracking · verification · bounded interfaces | become Governance · transfer ultimate accountability · create authority from nothing · exceed the granting boundary |
| **W4** Execution | existing execution / workflow / runtime surfaces | `DP-03 §14` | `PLAN → DELEGATE → EXECUTE → OBSERVE → VERIFY → ADAPT` | leave the authority envelope |
| **W5** Memory | bounded organizational continuity | `DP-03 §14`, `DP-04 §8.4` | preserve context · support continuity · evidence-backed state | become authority · create authorization · override canonical authority · invent decisions |
| **W6** Performance | **Optimization / Observation — DETECT-ONLY** | `DP-03 §8.3` | observe · measure · detect · report · evidence · improvement opportunities | authorize · approve · promote · decide · create governance or execution authority |
| **W7** Governance Boundary | existing human authority architecture | `DP-03 §8`, `P11-W7` | escalation paths | expand constitutional, Founder or Architect-reserved authority |

### 4.1 The prioritization boundary, carried forward explicitly

`DP-03 §7`: *"Performance evidence may inform Planning but does not become
Planning authority."* **`prioritization model`, `ranking model` and `decision
heuristics` remain Architect-reserved and unimplemented** — this authorization
does not reach them. `W2`'s *"bounded prioritization integration"* means
**consuming an authorized mechanism, not building the reserved one.**

---

## 5–6. Conformance to the issued architecture

**`DP-03` conformance:** every `W1`–`W7` row above is taken from `DP-03 §8`/`§14`
verbatim in substance. **No placement is broadened.**
**`DP-04` conformance:** `Goal` and `Plan` remain organizational concepts;
`Delegation` remains a governed organizational relation/record; both stay outside
the frozen Native Core; `OrganizationalState` remains a bounded projection.

## 7. Native Core #12 — prohibited

```text
NATIVE CORE = 11 FROZEN BOUNDARIES        NO NATIVE CORE #12
```

This authorization does **not** permit a twelfth subsystem, a new Native Core
entity, silent modification of frozen boundaries, or reinterpreting `DP-03`/
`DP-04` to manufacture a new Core home. **Organizational-layer construction is not
Native Core expansion** (`DP-03 §8.5`).

## 8. P12 boundary

**Not authorized:** `P12 Unified Operational State` · P12 system-wide integration
· P12 AIOS self-model · P12 system-wide verification.
**Allowed:** exposing interfaces a future integration may consume.
**`P11 ≠ P12`.**

## 9. Platform Organization boundary

**`P11 ≠ PD` · `P11 ≠ Platform Organization`.** P11 may operate over structures
produced through P10 and integrate with PD surfaces where required. It may not
redefine PD taxonomy, rewrite `PD-01`…`PD-10`, construct PD capabilities as P11
capabilities, or be treated as a replacement for Platform Organization.

## 10. P10 dependency boundary

P11 assumes the **certified** P10 Department Ecosystem. **This authorization does
not permit reconstructing P10** because P11 consumes it. An incomplete P10
dependency found during construction is `CLASSIFY → VERIFY → INTEGRATE if
authorized → ESCALATE if reserved`.

## 11–13. Authority hierarchy and what this instrument is not

```text
MISSION → CONSTITUTION → CANONICAL ARCHITECTURE → ARCHITECT DECISIONS
       → FOUNDER AUTHORIZATION → DELEGATED EXECUTION → IMPLEMENTATION → VERIFICATION
```

**Founder Authorization authorizes construction.** It does **not** replace
architecture or Architect Decisions, ratify `E11`, certify P11, or declare
completion or operationality.

> **`ARCHITECTURE DECISION ≠ FOUNDER AUTHORIZATION`.** `DP-03` and `DP-04` are
> issued; **their existence authorizes nothing.**

## 14. `E11` separation

> **This instrument does not ratify `E11`.** `CANDIDATE E11 ≠ RATIFIED E11`, and
> `DP-01` cannot convert candidates into ratified criteria. `E11` ratification is
> `DP-02`, a separate Founder Decision.

**One matter for `DP-02`, carried forward from `ACT-CC-P11-001`:** the eight
candidate `E11` criteria measure **capability only**. **None measures the negative
controls** — that the organization cannot self-authorize, that memory cannot
become authority, that failure cannot present as completion. `E10-06` carried
exactly that for Phase 10. `DP-03 §11`/`§12` now state those invariants
architecturally, so the gap is narrower — **but ratified criteria that never test
them would leave the strongest properties of this architecture unmeasured.**

---

## 15. Authorization scope matrix (`§15`)

| Surface | Existing `DP-01` | `DP-04` | `DP-03` | **Final `DP-01`** |
|---|---|---|---|---|
| W1 Coordination | **DOES NOT EXIST** | — | Workflow | **REQUIRED** |
| W2 Planning | **DOES NOT EXIST** | org layer | org Planning, mutable lifecycle | **REQUIRED** |
| W3 Delegation | **DOES NOT EXIST** | org relation/record | org relation/record | **REQUIRED** |
| W4 Execution | **DOES NOT EXIST** | — | bounded execution | **REQUIRED** |
| W5 Memory | **DOES NOT EXIST** | bounded projection | bounded continuity | **REQUIRED** |
| W6 Performance | **DOES NOT EXIST** | Observation | Optimization, detect-only | **REQUIRED** |
| W7 Governance | **DOES NOT EXIST** | authority boundary | human governance | **REQUIRED** |
| Native Core #12 | **DOES NOT EXIST** | NO | NO | **PROHIBITED** |
| P12 Unified State | **DOES NOT EXIST** | P12 | P12 | **PROHIBITED** |
| E11 Ratification | **DOES NOT EXIST** | — | — | **PROHIBITED** |

**The `?` column the Act asked me to resolve resolves to a uniform answer**, and
it is reported as such rather than filled with a plausible-looking prior text.

## 16. Scope diff — every row is `ADD`

| ID | Existing | Requirement | Class | Evidence |
|---|---|---|---|---|
| SD-01…SD-07 | **none** | `W1`–`W7` bounded per `DP-03 §8`/`§14` | **ADD** | issued bodies |
| SD-08 | **none** | Native Core #12 prohibition | **ADD** | `DP-03 §8.5`, `DP-04 §9` |
| SD-09 | **none** | P12 boundary | **ADD** | `DP-04 §10` |
| SD-10 | **none** | `E11` separation | **ADD** | `DP-03 §15` |
| SD-11 | **none** | prioritization boundary | **ADD** | `DP-03 §7` |
| SD-12 | **none** | `DP-01` identifier collision | **ESCALATE** | `§0.1` — Founder's to resolve or accept |

**No `PRESERVE`, `MODIFY` or `REMOVE` rows exist, because nothing existed.**

---

## 19. Negative controls (`§19`) — all present in this surface

```text
NC-01 no Native Core #12                     §7
NC-02 Planning cannot self-authorize         §4 W2
NC-03 Delegation cannot create authority     §4 W3
NC-04 Delegation ≠ Governance                §4 W3
NC-05 no transfer of ultimate accountability §4 W3
NC-06 Performance cannot authorize/decide    §4 W6
NC-07 Optimization detect-only               §4 W6, §4.1
NC-08 Memory cannot become authority         §4 W5
NC-09 OrgState ≠ P12 Unified State           §8
NC-10 P11 cannot authorize P12               §8
NC-11 no silent PD modification              §9
NC-12 Architecture ≠ Founder Authorization   §11–13
NC-13 Authorization ≠ E11 Ratification       §14
NC-14 candidate E11 stays candidate          §14
NC-15 autonomy ≠ constitutional expansion    §4 W7
NC-16 autonomy ≠ Founder expansion           §4 W7
NC-17 Architect-reserved stays reserved      §4.1
NC-18 uncertainty ≠ successful authorization §0
NC-19 document existence ≠ authorization     §11–13
NC-20 identifier existence ≠ decision body   §0, §0.1
```

## 20. Prove-me-wrong (`§20`)

| Test | Result |
|---|---|
| **A — Planning** | **PASS.** `§4 W2` states the mutable lifecycle as mandatory and explicitly bars constraint to a static declaration. Had I copied the P10 pattern uncritically this would have failed |
| **B — Delegation** | **PASS.** `§4 W3` bars becoming Governance, transferring accountability, or creating authority |
| **C — Performance** | **PASS.** `§4 W6` + `§4.1` bar authorize/approve/promote/decide and keep the reserved models out |
| **D — Core** | **PASS.** `§7` prohibits #12 explicitly |
| **E — P12** | **PASS.** `§8` prohibits construction, permits interface preparation only |
| **F — Founder** | **PASS.** `§21`–`§24` unfilled |
| **G — Architect** | **PASS.** Every scope row derives from `DP-03`/`DP-04`; neither is amended |

**One test deserves comment.** **Test A is the one that would have failed** if I
had prepared this surface before `DP-03` issued — my own `Option C`
recommendation would have authorized a static-record Planning surface. **The
architecture decision corrected that, and this surface carries the correction
rather than my original recommendation.**

---

## 21–24. FOUNDER-RESERVED — unfilled

```text
[FOUNDER CHOICE REQUIRED]
    [  ] AUTHORIZE P11 CONSTRUCTION over the scope in §4, and no wider
    [  ] AUTHORIZE WITH MODIFICATIONS — stating them
    [  ] WITHHOLD — stating the unsatisfied condition

[FOUNDER RATIONALE REQUIRED]

[FOUNDER EFFECTIVE DATE REQUIRED]

[FOUNDER SIGNATURE REQUIRED]
```

## 25. Final status

> # `P11 AUTHORIZATION = NOT ISSUED`

```text
P11 AUTHORIZED   = FALSE      P11 CONSTRUCTED = FALSE
E11 RATIFIED     = FALSE      P12 AUTHORIZED  = FALSE
NATIVE CORE MODIFIED = FALSE  NATIVE CORE BOUNDARIES = 11
```
