"""
Native Core `core/` region (Native Core Blueprint §3-§4).

Contains exactly one module boundary per frozen subsystem. No boundary may
exist without a ratified basis (Blueprint §2). Phase 3.1 establishes only
the Infrastructure boundary; the remaining ten boundaries are built in later
authorized stages, in the order fixed by the Native Core Implementation
Roadmap (§7).
"""
